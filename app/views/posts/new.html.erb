<% provide(:title, "New Post") %>

<div class="form-login">
	<h1>Spread the word!</h1>
	<%= form_for(:post, url: posts_path) do |f| %>
		<%= f.text_area :content, class: "form-control input-lg chat-input"%><br>
		<%= f.submit "Post", class: "btn btn-primary btn-md" %>
	<% end %>
</div>
